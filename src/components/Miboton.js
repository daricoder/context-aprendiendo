import {BotonEstilizado} from '../components/BotonEstilizado';

const Miboton = () => {
  return (
    <div style={{ background: "gray" }}>
      <p>SOY EL COMPONENTE BOTON</p>
      <BotonEstilizado />
    </div>
  );
};

export default Miboton;
